import { UpdateTaskDoneStatusUseCase } from '../UpdateTaskDoneStatusUseCase';

const updateTaskDoneStatusUseCase = new UpdateTaskDoneStatusUseCase();

describe.skip('UseCase: updateTaskDoneStatus', () => {
  test('Taskを完了にすると、Taskが完了状態になる', async () => {
    // given
  });

  test('Taskを未完了にすると、Taskが未完了状態になる', async () => {
    // given
  });
});
