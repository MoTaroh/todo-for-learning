import { UpdateTaskRemovedStatusUseCase } from '../UpdateTaskRemovedStatusUseCase';

const updateTaskRemovedStatusUseCase = new UpdateTaskRemovedStatusUseCase();

describe('UseCase: updateTaskRemovedStatus', () => {
  test('Taskを削除すると、Taskが削除状態になる', async () => {
    // given
  });

  test('Taskを復元すると、Taskが未削除状態になる', async () => {
    // given
  });
});
